[user]
    email = Felix.E.Huang@gmail.com
    name = Erystasius
[alias]
    di = diff
    ps = push
    pl = pull
    ci = commit
    st = status
    br = branch
    rb = rebase
    rl = reflog
    cf = config
    co = checkout	
    sm = submodule
    unstage = restore --staged :/
    throw = !git reset --hard HEAD && git clean -df
    lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
    ac = ! git add --all && git ci -m 'No-message commit [Generated by ac command]'
    cp = ! git ci -m 'No-message commit [Generated by ac command]' && git push
    acp = ! git ac && git push
	alias = ! git config --get-regexp ^alias\\. | sed -e s/^alias\\.// -e s/\\ /\\ =\\ /
[core]
    editor = vim
    # See https://git-scm.com/book/en/v2/Customizing-Git-Git-Configuration
    autocrlf = input
[push]
    # Push the current branch to its upstream branch, but refuses to push if the upstream branch’s name is different from the local one.
    default = simple
[fetch]
    # Prune remote-tracking branches no longer on remote
    prune = true
