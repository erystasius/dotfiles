- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.emacs.d: emacs.d
    ~/.vimrc: vimrc
    ~/.vim: vim
    ~/.bashrc: bashrc
    ~/.bash_profile: bash_profile
    ~/.inputrc: inputrc
    ~/.xprofile: xprofile
    ~/.ideavimrc: ideavimrc
    ~/.tmux.conf: tmux.conf
    /etc/aria2/aria2.conf: aria2.conf
    ~/.config/rofi/config: rofi.conf

